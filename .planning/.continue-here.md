---
context: between-milestones
phase: null
status: paused
last_updated: 2026-05-21
---

# Current State

Between v1.2 (complete) and v1.3 (planned, not yet started). All planning artifacts committed. All tests passing (222).

## Completed This Session

- Fixed `news-actions.ts` — Claude returning null for `context_summary` (commit ef93479f)
- Fixed `contradiction.ts` — null array fields from Claude tool response (commit f8fae359)
- Fixed `Array.isArray()` guards in `contradiction.ts`, `watchlist/generator.ts`, `WatchListTab.tsx` (commit 7b0b709d)
- Fixed test expectations in `parser.test.ts` and `price-actions.test.ts` (commit 2ef33960)
- Defined v1.3 Household Budget Tracker — Phases 16–19, 20 requirements (commit b22ba0f3)
- Set up GitHub remote: `https://github.com/Alcatraz0910/Investment-tool.git` on `main` branch

## Critical Pattern — Do Not Break

**Always use `Array.isArray(x) ? x : []` — never `x ?? []` — for Claude profile fields.**
Claude can return `{}` (object) for required array fields (`favoured_stocks`, `sector_focus`, `preferred_index_funds`). `{}` is truthy so `?? []` doesn't apply, but `.map()` crashes. This affects: `contradiction.ts`, `watchlist/generator.ts`, `WatchListTab.tsx`.

## v1.3 Scope (confirmed by user)

- **Standalone** Budget section (NOT wired to Watch List budgets)
- Multi-person household: user + partner each import bank CSVs
- AI categorises transactions → user confirms/overrides
- AI observations: cut-back suggestions, duplicate subscriptions
- Summary: income − outgoings = monthly surplus + month navigation
- No Open Banking / TrueLayer — manual CSV only
- Same AI guardrail: no "advise", "recommend", or "suggest"

## Next Action

```
/gsd-plan-phase 16
```

Phase 16: Budget Foundation — DB schema (`household_members`, `income_entries`, `transactions` tables in Supabase), new Budget page/tab in dashboard nav, household member + income entry UI.

## Required Reading

1. `.planning/STATE.md` — v1.3 milestone status
2. `.planning/ROADMAP.md` — Phases 16–19 definitions
3. `.planning/REQUIREMENTS.md` — HSLD, TIMP, TCAT, AINS, BSUM requirement groups
