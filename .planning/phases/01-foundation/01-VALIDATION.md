---
phase: 1
slug: foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-06
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | TypeScript compiler (`tsc --noEmit`) + manual browser verification |
| **Config file** | `tsconfig.json` (generated by create-next-app) |
| **Quick run command** | `npx tsc --noEmit` |
| **Full suite command** | `npm run build` |
| **Estimated runtime** | ~30 seconds (tsc) / ~60 seconds (build) |

> No automated test framework (Jest/Vitest) is installed in Phase 1. AUTH-01 and AUTH-02 are browser-flow requirements requiring a live Supabase project. Validation strategy: type compilation pass + manual browser smoke test.

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit`
- **After every plan wave:** Run `npm run build`
- **Before `/gsd-verify-work`:** `npm run build` green + manual auth smoke test pass
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1-01-01 | 01 | 1 | — | — | N/A | build | `npm run build` | ❌ W0 | ⬜ pending |
| 1-02-01 | 02 | 2 | AUTH-01 | T-1-01 | Supabase signInWithPassword called (not hand-rolled auth) | manual | `npm run dev` → /auth/login | ❌ W0 | ⬜ pending |
| 1-02-02 | 02 | 2 | AUTH-02 | T-1-02 | getUser() validates session server-side on every request | manual | Visit /dashboard logged out → redirects to /auth/login | ❌ W0 | ⬜ pending |
| 1-02-03 | 02 | 2 | AUTH-02 | T-1-02 | HttpOnly cookie prevents XSS session theft | manual | Refresh /dashboard after login — session persists | ❌ W0 | ⬜ pending |
| 1-03-01 | 03 | 2 | — | T-1-03 | RLS enabled on all tables; auth.uid() policies on user-scoped tables | type check | `npx tsc --noEmit` | ❌ W0 | ⬜ pending |
| 1-04-01 | 04 | 3 | — | — | Decimal imported from decimal.js for £ fields | type check | `npx tsc --noEmit` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- No test files to create — Phase 1 validation is type compilation + manual browser testing
- Supabase project must exist before any auth tests can run (user prerequisite, not a code gap)
- `npm run build` cannot run until scaffold is complete (sequential dependency)

*Existing infrastructure (tsc + Next.js build) covers all automated phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| User can sign up with email/password | AUTH-01 | Requires live Supabase project + browser | `npm run dev` → visit /auth/signup → enter email/password → submit → confirm redirect to /dashboard |
| User can log in with email/password | AUTH-01 | Requires live Supabase project + browser | `npm run dev` → visit /auth/login → enter credentials → submit → confirm redirect to /dashboard |
| Session persists after browser refresh | AUTH-02 | Requires browser state + live auth | Log in → refresh page → confirm still on /dashboard (not redirected to login) |
| Unauthenticated /dashboard redirects to /auth/login | AUTH-02 | Requires middleware + live session check | Visit /dashboard while logged out → confirm redirect to /auth/login |
| DB schema deployed with correct FK + RLS | D-05 | Requires Supabase console verification | Open Supabase SQL editor → run schema SQL → confirm all 8 tables exist with RLS enabled |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
