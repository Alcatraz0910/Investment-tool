---
phase: 12-watch-list-per-creator-budget
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pulse/tests/watch-budget.test.ts
  - pulse/tests/watchlist-generator.test.ts
  - pulse/tests/watch-actions.test.ts
autonomous: true
requirements:
  - WL-01
  - WL-03
  - WL-04

must_haves:
  truths:
    - "Three test stub files exist under pulse/tests/ and vitest can parse them without error"
    - "Each stub file exports the test suite (describe block) that later waves will fill with implementations"
    - "SQL migration snippet is documented and ready for user to run in Supabase SQL Editor"
  artifacts:
    - path: "pulse/tests/watch-budget.test.ts"
      provides: "WL-03 test stubs — saveCreatorMonthlyBudget validation"
      contains: "describe('saveCreatorMonthlyBudget'"
    - path: "pulse/tests/watchlist-generator.test.ts"
      provides: "WL-01 + WL-04 test stubs — buildWatchLists + calcShareQuantity"
      contains: "describe('buildWatchLists'"
    - path: "pulse/tests/watch-actions.test.ts"
      provides: "WL-02 test stubs — fetchTickerPrices integration"
      contains: "describe('watch-actions'"
  key_links:
    - from: "pulse/tests/watchlist-generator.test.ts"
      to: "pulse/src/lib/watchlist/generator.ts"
      via: "import { buildWatchLists, calcShareQuantity }"
      pattern: "buildWatchLists|calcShareQuantity"
    - from: "pulse/tests/watch-budget.test.ts"
      to: "pulse/src/app/dashboard/watchlist-actions.ts"
      via: "import { saveCreatorMonthlyBudget }"
      pattern: "saveCreatorMonthlyBudget"
---

<objective>
SQL migration documentation (user-run) + vitest test stubs for all three WL test files.

Purpose: Establish the Nyquist-compliant test harness before any implementation lands. Wave 1 and Wave 2 plans import from these stubs' target modules — having the stubs in place ensures `npx vitest run` remains green throughout.

Output:
- `pulse/tests/watch-budget.test.ts` — stub for WL-03 (saveCreatorMonthlyBudget)
- `pulse/tests/watchlist-generator.test.ts` — stubs for WL-01 + WL-04 (buildWatchLists, calcShareQuantity)
- `pulse/tests/watch-actions.test.ts` — stub for WL-02 (price-enriched watch list)
- SQL migration snippet embedded in this plan's output section (user copies to Supabase SQL Editor)
</objective>

<execution_context>
@C:/Users/dozan/Documents/AI Projects/Investing/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dozan/Documents/AI Projects/Investing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/PROJECT.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/ROADMAP.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/phases/12-watch-list-per-creator-budget/12-RESEARCH.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/phases/12-watch-list-per-creator-budget/12-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure pattern — read pulse/tests/extractor.test.ts for import style -->
<!-- vitest config: pulse/vitest.config.ts -->
<!-- All tests run with: cd pulse && npx vitest run -->

Existing test file pattern (from pulse/tests/extractor.test.ts):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
// vi.mock('@/...') for server-only modules
```

Target modules these stubs will import (created in Wave 1):
- `@/lib/watchlist/generator` — exports buildWatchLists, calcShareQuantity
- `@/app/dashboard/watchlist-actions` — exports saveCreatorMonthlyBudget
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run SQL migration in Supabase SQL Editor</name>
  <what-built>
    The following SQL adds the monthly_budget_gbp column to user_creators.
    This must be run before Wave 1 implementation — the server action writes to this column.
  </what-built>
  <how-to-verify>
    1. Open Supabase project → SQL Editor → New query
    2. Paste and run the following SQL exactly:

    ```sql
    ALTER TABLE public.user_creators
      ADD COLUMN IF NOT EXISTS monthly_budget_gbp NUMERIC(10, 2) NOT NULL DEFAULT 0;
    ```

    3. Confirm: no error, query returns "Success. No rows returned."
    4. Verify in Table Editor: user_creators table now has column monthly_budget_gbp (type numeric, default 0, not null).
  </how-to-verify>
  <resume-signal>Type "migration done" after the column appears in the Table Editor.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create vitest test stubs</name>
  <read_first>
    - pulse/vitest.config.ts (alias config — confirm @/ maps to src/)
    - pulse/tests/extractor.test.ts (import style, vi.mock pattern)
    - pulse/tests/creator-actions.test.ts (server action mock pattern)
  </read_first>
  <files>
    pulse/tests/watch-budget.test.ts
    pulse/tests/watchlist-generator.test.ts
    pulse/tests/watch-actions.test.ts
  </files>
  <action>
    Create three test stub files. Each file must:
    - Import from the target module path (Wave 1 will create these)
    - Mock server-only modules (supabase, next/headers) where needed
    - Have describe blocks with it.todo() stubs for each behavior under test
    - Pass vitest's parse/collect phase immediately (no red tests yet — stubs only)

    --- FILE 1: pulse/tests/watch-budget.test.ts ---

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    vi.mock('@/lib/supabase/server', () => ({
      createClient: vi.fn(),
    }))
    vi.mock('next/headers', () => ({ cookies: vi.fn() }))

    // saveCreatorMonthlyBudget is created in Wave 1 (12-02-PLAN.md)
    // import { saveCreatorMonthlyBudget } from '@/app/dashboard/watchlist-actions'

    describe('saveCreatorMonthlyBudget', () => {
      it.todo('returns success when budget is 0')
      it.todo('returns success when budget is 20000')
      it.todo('returns error when budget is negative')
      it.todo('returns error when budget exceeds 20000')
      it.todo('returns error when user is not authenticated')
    })
    ```

    --- FILE 2: pulse/tests/watchlist-generator.test.ts ---

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { Decimal } from 'decimal.js'

    // buildWatchLists and calcShareQuantity are created in Wave 1 (12-02-PLAN.md)
    // import { buildWatchLists, calcShareQuantity } from '@/lib/watchlist/generator'

    describe('buildWatchLists', () => {
      it.todo('returns empty array when creators array is empty')
      it.todo('returns items from profile_stable.favoured_stocks when ticker is non-null')
      it.todo('returns items from profile_stable.preferred_index_funds when ticker is non-null')
      it.todo('includes items from profile_latest.favoured_stocks when profile_latest is non-null')
      it.todo('filters out favoured_stocks entries where ticker is null')
      it.todo('sets hasProfile=false when profileStable is null')
      it.todo('sorts items: high conviction before medium before low')
    })

    describe('calcShareQuantity', () => {
      it.todo('returns quantity=0 and remainder=budget when price is 0')
      it.todo('returns quantity=0 and remainder=budget when price is negative')
      it.todo('floors to whole shares — e.g. £500 / £47.23 = 10 shares')
      it.todo('calculates remainder correctly after floor division')
      it.todo('does not use native JS division — uses Decimal.div().floor()')
    })
    ```

    --- FILE 3: pulse/tests/watch-actions.test.ts ---

    ```typescript
    import { describe, it, expect, vi } from 'vitest'

    vi.mock('@/lib/supabase/server', () => ({
      createClient: vi.fn(),
    }))
    vi.mock('next/headers', () => ({ cookies: vi.fn() }))

    // fetchTickerPrices is already in actions.ts (Phase 8) — tests here verify
    // the WatchListTab integration pattern (prices keyed by ticker, GBp→GBP done server-side)

    describe('watch-actions — fetchTickerPrices integration', () => {
      it.todo('returns a Record<string, number> keyed by ticker')
      it.todo('prices are in £ (GBp already divided by 100 server-side)')
      it.todo('missing tickers return undefined (not null, not 0)')
    })
    ```

    IMPORTANT: Do NOT uncomment the import lines — the target modules do not exist yet.
    The files must be parseable by vitest without import errors.
    Run `cd pulse && npx vitest run` after creating all three files — all stubs must show as "todo" (yellow), zero failures.
  </action>
  <verify>
    <automated>cd pulse && npx vitest run tests/watch-budget.test.ts tests/watchlist-generator.test.ts tests/watch-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pulse/tests/watch-budget.test.ts exists and contains `describe('saveCreatorMonthlyBudget'`
    - pulse/tests/watchlist-generator.test.ts exists and contains `describe('buildWatchLists'`
    - pulse/tests/watchlist-generator.test.ts contains `describe('calcShareQuantity'`
    - pulse/tests/watch-actions.test.ts exists and contains `describe('watch-actions'`
    - `cd pulse && npx vitest run tests/watch-budget.test.ts tests/watchlist-generator.test.ts tests/watch-actions.test.ts` exits 0 (all todo, zero failures)
  </acceptance_criteria>
  <done>All three test stub files exist, parse cleanly, and vitest exits 0 with all tests in todo state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Supabase SQL Editor | User manually executes migration SQL — no app code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | SQL migration (user-run) | accept | Migration is additive-only (ADD COLUMN IF NOT EXISTS); no data loss risk; user controls their own Supabase project |
</threat_model>

<verification>
- SQL migration run confirmed by user (checkpoint gate)
- `cd pulse && npx vitest run tests/watch-budget.test.ts tests/watchlist-generator.test.ts tests/watch-actions.test.ts` exits 0
- Zero failing tests (todo state is acceptable)
</verification>

<success_criteria>
- monthly_budget_gbp column exists in user_creators table (NUMERIC(10,2) NOT NULL DEFAULT 0)
- Three test stub files exist under pulse/tests/
- Full vitest suite still green: `cd pulse && npx vitest run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/12-watch-list-per-creator-budget/12-01-SUMMARY.md`
</output>
