---
context: phase
phase: 07-csv-portfolio-import
status: human_verification_pending
last_updated: 2026-05-15
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from failure this session._

<current_state>
Phase 7 fully executed (4/4 plans, 2 waves). Two post-execution bugs fixed during live testing.
Stock name field added. Awaiting: (1) Supabase migration for name column, (2) human UAT.
</current_state>

<completed_work>

- 07-01: CSV Parser Utilities — PapaParse, broker detection, GBX→£, 24/24 tests ✓
- 07-02: importHoldings server action — merge/replace modes, auth-guarded ✓
- 07-03: ImportCSVModal — 3-step modal (Upload→Map→Preview+Confirm), 355 lines ✓
- 07-04: PortfolioTab — Import CSV button wired; smoke test skipped (Supabase unreachable) ✓
- BUG FIX (8e5fad0): HL CSV has 8+ metadata rows above data table — added extractDataSection()
  to find "Code" row; fixed HL preset (Code/Description/Units/Value(£), gbx: false)
- BUG FIX (14fbdfc): HL value column uses commas (4,250.00) — strips [,£] before Supabase insert
- FEAT (5633dd4): Stock name field — nullable TEXT, all layers updated
</completed_work>

<remaining_work>

HUMAN ACTIONS FIRST:
1. Run in Supabase SQL editor: ALTER TABLE public.holdings ADD COLUMN IF NOT EXISTS name TEXT;
   (file: pulse/supabase/migrations/add_holding_name.sql)

UAT (07-HUMAN-UAT.md — 3 items):
2. Full generic CSV import flow smoke test
3. HL CSV auto-detect + GBX preview rendering
4. AJ Bell auto-detect flow

NEXT FEATURE (new session):
5. AI extraction — Claude API first-pass column suggestions
   (user wants: suggestion mode, not fully automatic)
   After UAT: /gsd-verify-work → phase 7 complete → /gsd-discuss-phase 8
</remaining_work>

<decisions_made>

- HL CSV: extractDataSection() scans for "Code" as first cell, slices from there
- HL columns: Code→ticker, Description→name, Units→quantity, Value(£)→value, gbx: false
- Value cleaning: strip [,£] in parseRows AND classifyRow before any numeric parsing
- name: nullable TEXT, no default — existing holdings unaffected
- AI extraction scope: first-pass suggestion (Claude proposes, user reviews before confirming)
</decisions_made>

<blockers>

- Supabase ENOTFOUND in this dev environment — live connection needed for UAT
- name column migration not yet applied to live DB
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-csv-portfolio-import/07-HUMAN-UAT.md` — 3 pending smoke test items
2. `pulse/supabase/migrations/add_holding_name.sql` — run before testing
3. `pulse/src/lib/csv/parser.ts` — extractDataSection, HL preset, value cleaning

## Infrastructure State
- All changes committed to master (last commit: 5633dd4)
- No uncommitted changes
- Dev server: not running

<context>
Session: executed all 4 plans, discovered HL CSV format wrong in the plan (metadata header rows, wrong column names, comma-formatted values), fixed all three bugs during live testing. Added stock name as enhancement. AI extraction scoped but deferred — user wants first-pass suggestion mode.
</context>

<next_action>
1. User runs Supabase migration for name column
2. User tests CSV import with live Supabase, fills 07-HUMAN-UAT.md
3. After UAT passes: /gsd-verify-work
4. New feature: AI extraction (new session — implement first-pass Claude column suggestions)
</next_action>
