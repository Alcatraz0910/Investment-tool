---
context: phase
phase: 07-csv-portfolio-import
task: 0
total_tasks: 0
status: ready_to_plan
last_updated: 2026-05-13T00:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session. Section retained for structure._

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

_No anti-patterns discovered this session. Remove this table if still empty after planning._

<current_state>
Phase 7: CSV Portfolio Import — UI-SPEC complete, no PLAN.md yet.

This session completed:
1. `/gsd-discuss-phase 7` (prior session) → `07-CONTEXT.md` written with D-01 through D-13 locked
2. `/gsd-plan-phase 7` → blocked at UI gate (Phase 7 has frontend components, no UI-SPEC existed)
3. `/gsd-ui-phase 7` → `07-UI-SPEC.md` created and verified (6/6 dimensions PASS, approved 2026-05-13)

Next immediate action: `/gsd-plan-phase 7`
The UI gate will now pass (07-UI-SPEC.md exists). The planner will pick it up automatically.
</current_state>

<completed_work>

- [x] `07-CONTEXT.md` — 13 decisions locked (D-01 through D-13), canonical refs defined
- [x] `07-UI-SPEC.md` — UI design contract approved; 6/6 checker dimensions passed
  - ImportCSVModal: 3-step flow (Upload → Map Columns → Preview+Confirm), 2-step for HL/AJ Bell
  - Drop zone, auto-detect banner, column mapping table, preview table all fully specified
  - 30+ copywriting strings defined; zero forbidden words
  - Glassmorphism + Phase 6 tokens extended without contradiction
  - Animation contract: modal mount scale(0.97)+y(8), step transitions reuse AnimatedTabPanel variants
</completed_work>

<remaining_work>

- [ ] `/gsd-plan-phase 7` — create PLAN.md files (UI-SPEC will be picked up automatically now)
  - Optional: run research first when prompted (Phase 7 is well-understood; skip is reasonable)
- [ ] `/gsd-execute-phase 7` — after plans approved
- [ ] `/gsd-verify-work` — after execution
</remaining_work>

<decisions_made>

- **Client-side CSV parsing via PapaParse** — avoids Next.js 15.5 1 MB server action body limit; `bom: true` required for Excel-generated CSVs
- **GBX→£ conversion tied to HL preset only** — silent, `new Decimal(v).div(100)` — no user toggle
- **Multi-step modal pattern** — consistent with HoldingModal; Escape/backdrop close; focus trap; focus returns to trigger on close
- **Merge mode** — upsert on `(user_id, ticker)`, preserves `isFillTicker` and `category` on matched rows
- **Replace mode** — DELETE all holdings for user, INSERT valid CSV rows; amber warning banner in step 3
- **Invalid rows skipped silently** — shown with warning icon in preview (opacity-60), don't block valid rows
- **Status colors** — `text-green-400` Valid / `text-amber-400` Duplicate / `text-red-400` Invalid (Tailwind built-ins, not new tokens)
- **No category column → default "Stocks"** — user can edit per-holding after import via existing Edit modal
</decisions_made>

<blockers>
None. Phase 7 is self-contained (no external services, no new infra dependencies).
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-csv-portfolio-import/07-CONTEXT.md` — 13 locked decisions (D-01 through D-13); canonical refs; integration points
2. `.planning/phases/07-csv-portfolio-import/07-UI-SPEC.md` — approved UI contract; all component specs, copywriting, animations
3. `CLAUDE.md` — `decimal.js` mandate, no financial advice language, bare tickers only
4. `.planning/REQUIREMENTS.md` — CSV-01 through CSV-06

## Critical Anti-Patterns (do NOT repeat these)
_None discovered this session._

## Infrastructure State
- No dev server running
- No background processes
- `07-UI-SPEC.md` committed as WIP (this commit)

<context>
Phase 7 is in excellent shape for planning. CONTEXT.md has 13 precise locked decisions covering every aspect of the import flow. UI-SPEC adds the visual/interaction layer on top. The planner has everything it needs.

The phase is self-contained — no new external services, no schema migrations expected (holdings table already exists from Phase 2), no env vars needed. PapaParse needs to be added as an npm dependency if not already present.

Key things to watch during planning:
- `importHoldings(rows, mode)` server action needs to go in `pulse/src/app/dashboard/actions.ts` alongside existing holding actions
- `PortfolioTab.tsx` needs "Import CSV" button + modal mount — check current state of that file
- After import, `router.refresh()` or `revalidatePath` is needed so parent RSC re-fetches holdings
- `bom: true` in PapaParse config is easy to forget — make sure it's in the plan
</context>

<next_action>
Start with: `/gsd-plan-phase 7`
The UI gate will now pass. When prompted about research, "Skip research" is reasonable given the depth of CONTEXT.md (D-01 through D-13 cover the implementation thoroughly). Proceed to planner directly.
</next_action>
