---
phase: 15-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pulse/src/app/globals.css
  - pulse/src/app/layout.tsx
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "Geist and Geist Mono fonts load via next/font and are applied to the HTML element"
    - "CSS vars --font-geist and --font-geist-mono exist in globals.css @theme block"
    - "All existing @theme color tokens are preserved unchanged"
    - "The body uses Geist as default font (no more Inter)"
  artifacts:
    - path: "pulse/src/app/globals.css"
      provides: "Extended @theme block with font vars"
      contains: "--font-geist"
    - path: "pulse/src/app/layout.tsx"
      provides: "Geist + Geist Mono loaded via next/font/google with variable mode"
      contains: "Geist_Mono"
  key_links:
    - from: "pulse/src/app/layout.tsx"
      to: "pulse/src/app/globals.css"
      via: "--font-geist and --font-geist-mono CSS vars injected on <html> element"
      pattern: "geist\\.variable.*geistMono\\.variable"
---

<objective>
Load Geist and Geist Mono fonts, extend the @theme design token block in globals.css.

Purpose: Unblocks all subsequent plans. Every primitive and tab component in Waves 1–3 depends on --font-geist / --font-geist-mono CSS vars and the existing color tokens being present and stable.

Output: Updated globals.css (font vars added), updated layout.tsx (Inter replaced with Geist + Geist Mono in variable mode).
</objective>

<execution_context>
@C:/Users/dozan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dozan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/PROJECT.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/ROADMAP.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/phases/15-visual-redesign/15-UI-SPEC.md
@C:/Users/dozan/Documents/AI Projects/Investing/.planning/phases/15-visual-redesign/15-PATTERNS.md

<interfaces>
<!-- Current globals.css @theme block (from 15-PATTERNS.md) -->
```css
@theme {
  --color-surface: #2C2C2E;
  --color-base: #1C1C1E;
  --color-border: #3A3A3C;
  --color-accent: #6366F1;
  --color-accent-hover: #818CF8;
}
```

<!-- Current layout.tsx font loading (from 15-PATTERNS.md) -->
```tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend globals.css @theme with font vars</name>
  <files>pulse/src/app/globals.css</files>
  <read_first>
    - pulse/src/app/globals.css — read current @theme block and full file to avoid overwriting anything
  </read_first>
  <action>
Add two font variable declarations inside the existing @theme {} block. EXTEND — do not replace or remove any existing property.

Target @theme block after edit:
```css
@theme {
  --color-surface: #2C2C2E;
  --color-base: #1C1C1E;
  --color-border: #3A3A3C;
  --color-accent: #6366F1;
  --color-accent-hover: #818CF8;
  /* Phase 15 — typography */
  --font-geist: 'Geist', sans-serif;
  --font-geist-mono: 'Geist Mono', monospace;
}
```

No other changes to globals.css. Tailwind v4 CSS-first config — no tailwind.config.ts needed.
  </action>
  <verify>
    <automated>grep -c "\-\-font-geist-mono" "pulse/src/app/globals.css"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--font-geist: 'Geist', sans-serif;`
    - globals.css contains `--font-geist-mono: 'Geist Mono', monospace;`
    - globals.css still contains `--color-accent: #6366F1`
    - globals.css still contains `--color-base: #1C1C1E`
    - globals.css still contains `--color-surface: #2C2C2E`
    - globals.css still contains `--color-border: #3A3A3C`
    - globals.css still contains `--color-accent-hover: #818CF8`
  </acceptance_criteria>
  <done>globals.css @theme block contains all 7 properties (5 existing + 2 new font vars); no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Inter with Geist + Geist Mono in layout.tsx</name>
  <files>pulse/src/app/layout.tsx</files>
  <read_first>
    - pulse/src/app/layout.tsx — read full file to capture current metadata, body classes, and any other imports
  </read_first>
  <action>
Replace the Inter import and usage with Geist and Geist_Mono loaded in `variable` mode from `next/font/google`. The variable mode injects CSS custom properties onto the HTML element, linking next/font to the @theme vars declared in globals.css.

Required changes:
1. Replace `import { Inter } from 'next/font/google'` with:
   ```tsx
   import { Geist, Geist_Mono } from 'next/font/google'
   ```
2. Replace the `inter` constant with two font constants:
   ```tsx
   const geist = Geist({ subsets: ['latin'], variable: '--font-geist' })
   const geistMono = Geist_Mono({ subsets: ['latin'], variable: '--font-geist-mono' })
   ```
3. Update the RootLayout `<html>` element to inject both font variables:
   ```tsx
   <html lang="en" className={`${geist.variable} ${geistMono.variable}`}>
   ```
4. Update `<body>` to use the Geist variable as default font:
   ```tsx
   <body className="font-[var(--font-geist)]">
   ```
   (Keep any other existing body classes; only add the font class if not already present.)
5. Remove any reference to `inter` (`inter.className` or similar) from the html/body elements.

Do NOT remove Metadata import, existing metadata export, children prop, or any other part of the layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p pulse/tsconfig.json 2>&1 | grep -c "layout.tsx" || echo "0 errors in layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { Geist, Geist_Mono } from 'next/font/google'`
    - layout.tsx contains `variable: '--font-geist'`
    - layout.tsx contains `variable: '--font-geist-mono'`
    - layout.tsx contains `geist.variable` and `geistMono.variable` on the `<html>` element className
    - layout.tsx does NOT contain `Inter` or `inter`
    - layout.tsx still exports `metadata` (Metadata type)
    - npx tsc --noEmit exits 0 (no new TS errors introduced)
  </acceptance_criteria>
  <done>layout.tsx loads Geist + Geist Mono in variable mode; --font-geist and --font-geist-mono are injected on the html element; Inter is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Font CDN → browser | next/font downloads fonts at build time; no runtime CDN request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | globals.css CSS vars | accept | CSS vars are public by design; no secrets in token values |
| T-15-01-02 | Tampering | @theme block extension | accept | Additive change only; existing tokens verified in acceptance criteria |
</threat_model>

<verification>
From project root:
1. `grep --include="*.css" -r "\-\-font-geist" pulse/src/` — returns globals.css with both vars
2. `grep "Geist_Mono" pulse/src/app/layout.tsx` — returns the import line
3. `grep "inter" pulse/src/app/layout.tsx` — returns 0 lines (Inter removed)
4. `cd pulse && npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- Geist and Geist Mono are the active fonts for the application
- --font-geist and --font-geist-mono CSS vars are defined in @theme and injected on the html element
- All 5 existing color tokens in @theme are unchanged
- npx tsc --noEmit exits 0 after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-redesign/15-01-SUMMARY.md`
</output>
