---
phase: 05-plan-generator
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pulse/src/types/index.ts
autonomous: false
requirements:
  - PLAN-01
  - PLAN-02
must_haves:
  truths:
    - "holdings table has an is_fill_ticker column (BOOLEAN NOT NULL DEFAULT FALSE)"
    - "A partial unique index prevents two fill tickers per (user_id, category)"
    - "Holding type in types/index.ts includes isFillTicker: boolean"
    - "BuyListItem type in types/index.ts includes allocationGapPct: number"
  artifacts:
    - path: "pulse/src/types/index.ts"
      provides: "Extended Holding and BuyListItem domain types"
      contains: "isFillTicker: boolean"
    - path: ".planning/phases/01-foundation/schema.sql"
      provides: "Reference schema — migration run separately via SQL editor"
  key_links:
    - from: "pulse/src/types/index.ts"
      to: "pulse/src/lib/plan/generator.ts"
      via: "HoldingWithFillTicker extends Holding — generator.ts imports from types"
      pattern: "isFillTicker"
    - from: "Supabase SQL editor"
      to: "public.holdings"
      via: "ALTER TABLE + CREATE UNIQUE INDEX"
      pattern: "is_fill_ticker"
---

<objective>
Add `is_fill_ticker` to the `holdings` table via manual SQL migration, and extend the TypeScript types to include `isFillTicker` on `Holding` and `allocationGapPct` on `BuyListItem`.

Purpose: All downstream Wave 1+ code reads `is_fill_ticker` from the DB and uses `allocationGapPct` on buy list items. Both the schema column and the type extension must exist before any plan code compiles.

Output:
- Schema: `public.holdings.is_fill_ticker BOOLEAN NOT NULL DEFAULT FALSE` with partial unique index
- Types: `Holding.isFillTicker: boolean` and `BuyListItem.allocationGapPct: number`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/schema.sql
@.planning/phases/05-plan-generator/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Run schema migration in Supabase SQL editor</name>
  <files>N/A — no code files; SQL run in Supabase dashboard</files>
  <read_first>
    - .planning/phases/01-foundation/schema.sql (verify holdings table structure before migrating)
    - .planning/phases/05-plan-generator/05-CONTEXT.md §D-02 (decision: boolean column on holdings)
  </read_first>
  <what-built>
    No code exists yet — this task runs the migration SQL that all Wave 1+ code depends on.
    The SQL adds `is_fill_ticker BOOLEAN NOT NULL DEFAULT FALSE` to `public.holdings`
    and creates a partial unique index enforcing at most one fill ticker per (user_id, category).
  </what-built>
  <how-to-verify>
    1. Open Supabase dashboard → SQL Editor → New query
    2. Paste and run the following SQL:

    ```sql
    ALTER TABLE public.holdings
      ADD COLUMN is_fill_ticker BOOLEAN NOT NULL DEFAULT FALSE;

    CREATE UNIQUE INDEX holdings_one_fill_ticker_per_category
      ON public.holdings (user_id, category)
      WHERE is_fill_ticker = TRUE;
    ```

    3. Go to Table Editor → holdings table
    4. Confirm column `is_fill_ticker` appears with type `bool`, nullable: false, default: false
    5. Run this verification query in SQL Editor:
       ```sql
       SELECT column_name, data_type, is_nullable, column_default
       FROM information_schema.columns
       WHERE table_schema = 'public'
         AND table_name = 'holdings'
         AND column_name = 'is_fill_ticker';
       ```
       Expected: one row with `data_type = boolean`, `is_nullable = NO`, `column_default = false`
    6. Verify the index exists:
       ```sql
       SELECT indexname, indexdef
       FROM pg_indexes
       WHERE tablename = 'holdings'
         AND indexname = 'holdings_one_fill_ticker_per_category';
       ```
       Expected: one row with `indexdef` containing `WHERE (is_fill_ticker = true)`
  </how-to-verify>
  <resume-signal>Type "migration done" once both SQL statements succeed and verification queries return expected rows.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend TypeScript types — Holding.isFillTicker and BuyListItem.allocationGapPct</name>
  <files>pulse/src/types/index.ts</files>
  <read_first>
    - pulse/src/types/index.ts (read current Holding and BuyListItem interfaces in full before editing)
  </read_first>
  <action>
    Edit `pulse/src/types/index.ts`. Make exactly two additions:

    1. In the `Holding` interface (currently ends with `updatedAt: Date`), add the field:
       ```typescript
       isFillTicker: boolean   // TRUE = preferred buy target for this category (D-01, Phase 5)
       ```
       Place it between `currentValue` and `createdAt` for logical grouping (value fields first, then timestamps).

    2. In the `BuyListItem` interface (currently has `ticker`, `category`, `amountGbp`, `rationale`), add the field:
       ```typescript
       allocationGapPct: number   // % of category gap this purchase partially closes (PLAN-02)
       ```
       Place it between `amountGbp` and `rationale`.

    Do NOT modify any other type. Do NOT change existing field names, order of other fields, or comments. The file must continue to compile — both fields are non-optional (required by generator logic).

    Per D-01 (05-CONTEXT.md): `isFillTicker` maps to `is_fill_ticker` in the DB. All DB reads in page.tsx must be updated in Wave 3 to include this column — note this for Wave 3 implementer.
  </action>
  <verify>
    <automated>cd pulse && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isFillTicker" pulse/src/types/index.ts` returns exactly one line inside the `Holding` interface
    - `grep "allocationGapPct" pulse/src/types/index.ts` returns exactly one line inside the `BuyListItem` interface
    - `cd pulse && npx tsc --noEmit` exits 0 (no compile errors introduced by this change)
    - The `Holding` interface still contains all 8 original fields plus `isFillTicker`
    - The `BuyListItem` interface still contains all 4 original fields plus `allocationGapPct`
  </acceptance_criteria>
  <done>
    types/index.ts compiles cleanly with both new fields. No existing fields removed or renamed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor → Supabase DB | Manual DDL run by authenticated admin; no user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | holdings.is_fill_ticker partial index | mitigate | Partial unique index enforced at DB level — prevents two fill tickers per (user_id, category) even if app-level logic has a bug |
| T-05-01-02 | Elevation of Privilege | Migration SQL run without verification | accept | Migration run manually by developer in authenticated Supabase dashboard; no user-supplied SQL; low risk |
</threat_model>

<verification>
After both tasks complete:
1. `grep "isFillTicker" pulse/src/types/index.ts` → one match in Holding interface
2. `grep "allocationGapPct" pulse/src/types/index.ts` → one match in BuyListItem interface
3. `cd pulse && npx tsc --noEmit` exits 0
4. Supabase SQL editor: `SELECT COUNT(*) FROM information_schema.columns WHERE table_name='holdings' AND column_name='is_fill_ticker'` → 1
</verification>

<success_criteria>
- holdings table has is_fill_ticker BOOLEAN NOT NULL DEFAULT FALSE
- Partial unique index holdings_one_fill_ticker_per_category exists and is WHERE is_fill_ticker = TRUE
- Holding type has isFillTicker: boolean
- BuyListItem type has allocationGapPct: number
- TypeScript compiles cleanly (tsc --noEmit exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/05-plan-generator/05-01-SUMMARY.md`
</output>
