---
context: phase
phase: 03-transcript-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-07T12:00:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: js-tiktoken WASM enc.decode() — crashes in this environment — ONLY use char-based chunking; never call enc.decode()

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| js-tiktoken enc.decode() | `enc.decode(Uint32Array)` throws "The 'list' argument must be an instance of SharedArrayBuffer, ArrayBuffer or ArrayBufferView" inside WASM Buffer.concat — happened 3 times across different fix attempts (TextDecoder wrap, fresh Uint32Array, Array.from) | blocking | chunker.ts now uses char-based splitting only (4 chars/token) — do NOT reintroduce enc.decode() or any call to the js-tiktoken decode path |
| Generic route error mask | route.ts returns hardcoded "YouTube API error — partial progress saved" for ALL pipeline errors, hiding the real cause | advisory | Always check `refresh_jobs.error` column in Supabase SQL Editor for the real error message |

<current_state>
Phase 3: Transcript Pipeline — all 7 plans code-complete, waiting on human verification.

Plan 03-07 (Dashboard Integration) is at Task 3 — human checkpoint. Tasks 1-2 are committed (page.tsx + creators-tab.tsx wired). The chunker has been fixed 3 times; the final fix (commit 3340762) replaces WASM-based tokenisation with character-based chunking entirely. User has NOT yet confirmed whether this fix resolves the Refresh pipeline error.

Dev server is running at http://localhost:3001 (port 3000 in use by a separate process).
</current_state>

<completed_work>
- 03-01: Env vars, npm packages, migration.sql, Pinecone index setup — ✓ committed
- 03-02: Supabase service client, YouTube client, OpenAI client, Pinecone client — ✓ committed
- 03-03: UserCreator.lastRefreshedAt, RefreshJob type, chunkText() — ✓ committed
- 03-04: transcript-pipeline.ts (323 lines, full idempotency) — ✓ committed
- 03-05: POST + GET /api/refresh/[creatorId]/route.ts — ✓ committed
- 03-06: RefreshButton (192 lines) + TranscriptList (134 lines) — ✓ committed
- 03-07 Task 1: page.tsx server data fetch extended — ✓ committed (0a2dbb6)
- 03-07 Task 2: creators-tab.tsx wired with RefreshButton + TranscriptList — ✓ committed (fe2cae3)
- Chunker fix 3: char-based splitting replaces WASM decode — ✓ committed (3340762)
- Pinecone guard: `if (vectors.length === 0) continue` — ✓ committed (b535c1c)
</completed_work>

<remaining_work>
- 03-07 Task 3: Human verification — user tests Refresh in browser; reports pass or failure
- If pass: close 03-07 checkpoint, spawn gsd-verifier, mark phase 3 complete in ROADMAP/STATE
- If fail: check refresh_jobs.error SQL again, debug next error
- Commit ROADMAP.md + STATE.md tracking updates (uncommitted)
- schema.sql has a pre-existing uncommitted change (from git status at session start) — check before committing
</remaining_work>

<decisions_made>
- js-tiktoken enc.decode() replaced with char-based chunking (4 chars/token ≈ token count). 500 tokens × 4 chars = 2000 chars max per chunk — well within OpenAI's 8191-token limit.
- Pinecone guard `if (vectors.length === 0) continue` added to transcript-pipeline.ts step 6.
- Supabase and Pinecone SDK type errors resolved with `(svc as any)` cast — consistent with existing creator-actions.ts pattern in codebase.
- YouTube channel resolution uses 3-strategy fallback: /channel/UCxxx → forHandle → forUsername.
- All server-only modules start with `import 'server-only'`; no NEXT_PUBLIC_ prefix on any secret key.
</decisions_made>

<blockers>
- Latest chunker fix (3340762) not yet confirmed working — user needs to click Refresh and observe success
- If Refresh still fails: run `SELECT status, step, error FROM public.refresh_jobs ORDER BY updated_at DESC LIMIT 1;` in Supabase SQL Editor for real error
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-transcript-pipeline/03-07-PLAN.md` — integration plan, human checkpoint details
2. `.planning/phases/03-transcript-pipeline/03-CONTEXT.md` — user decisions (D-03 through D-13)
3. `.planning/phases/03-transcript-pipeline/03-RESEARCH.md` — pitfalls, idempotency rules
4. `pulse/src/lib/pipeline/transcript-pipeline.ts` — current pipeline state
5. `pulse/src/lib/pipeline/chunker.ts` — current (fixed) chunker implementation

## Infrastructure State
- Dev server: running at http://localhost:3001 (started with `cd pulse && npm run dev`)
- Supabase: migration.sql applied — `user_creators.last_refreshed_at` + `public.refresh_jobs` table exist with RLS
- Pinecone: index created (name per PINECONE_INDEX_NAME env var, dim=1536, cosine)
- All 5 env vars in pulse/.env.local: YOUTUBE_API_KEY, OPENAI_API_KEY, PINECONE_API_KEY, PINECONE_INDEX_NAME, SUPABASE_SERVICE_ROLE_KEY

## Uncommitted Files
- `.planning/ROADMAP.md` — wave 5/plan 03-07 checkbox not ticked (pending human approval)
- `.planning/STATE.md` — phase 3 in-progress state
- `.planning/phases/01-foundation/schema.sql` — pre-existing uncommitted change, check before committing

<context>
All the code is done. The only thing left is confirming the Refresh button works end-to-end after the third chunker fix. Once the user says "approved", spawn a fresh gsd-executor to create the final 03-07 SUMMARY.md, then spawn gsd-verifier for the phase VERIFICATION.md, then mark the phase complete.

Do NOT try to resume the checkpoint agent (a5a976d03b3d90e32) — it's from the previous session. Spawn a fresh executor for the 03-07 SUMMARY.md and checkpoint closure instead.
</context>

<next_action>
Ask the user: "Did the Refresh work after the latest fix (commit 3340762)?" If yes → proceed to close 03-07 and verify phase. If no → run the Supabase SQL query to get the new error.
</next_action>
