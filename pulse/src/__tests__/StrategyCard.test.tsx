/**
 * UI-05: StrategyCard renders confidence score, lastRefreshedAt, contradiction badge.
 * Stubs — implementations added in plan 06-05.
 */
import { describe, it } from 'vitest'

describe('StrategyCard (UI-05)', () => {
  it.todo('renders confidence score as "Confidence: N%"')
  it.todo('renders lastRefreshedAt as "Last refreshed {date}"')
  it.todo('renders contradiction badge "Strategy conflict detected" when hasContradiction is true')
  it.todo('does NOT render contradiction badge when hasContradiction is false')
  it.todo('renders glassmorphism card class backdrop-blur-xl bg-white/5 border-white/10')
})
